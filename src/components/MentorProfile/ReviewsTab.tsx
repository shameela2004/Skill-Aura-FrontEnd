import React from "react";
export default function ReviewsTab({mentorId}: {mentorId: number}) {
  // Fetch reviews if implemented
  return (
    <div>
      <span className="text-gray-400">No reviews available.</span>
    </div>
  );
}
